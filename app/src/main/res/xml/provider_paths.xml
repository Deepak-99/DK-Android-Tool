<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Internal app files -->
    <files-path
        name="internal_files"
        path="." />
    
    <!-- Cache directory -->
    <cache-path
        name="cache"
        path="." />
    
    <!-- External files -->
    <external-path
        name="external"
        path="." />
    
    <!-- External files in app-specific directory -->
    <external-files-path
        name="external_files"
        path="." />
    
    <!-- External cache -->
    <external-cache-path
        name="external_cache"
        path="." />
    
    <!-- External media -->
    <external-media-path
        name="external_media"
        path="." />
</paths> 